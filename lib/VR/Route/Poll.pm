package VR::Route::Photo;

use common::sense;
use Dancer ':syntax';
use Dancer::Plugin::Database;

prefix '/poll';


get '/view' => sub {
    redirect '/';
};


get '/view' => sub {
    redirect '/';
};


get '/result' => sub {
    redirect '/';
};

true;
